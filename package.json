{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^8.1.1",
    "purescript": "^0.8.5",
    "purescript-psa": "^0.3.6",
    "rimraf": "^2.5.2"
  }
}
